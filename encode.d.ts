/**
 * Encode the given object to URI Component encoded query string.
 *
 * @param object The object to encode.
 * @param keepArrayIndex Whether to keep array index.
 * @returns Returns the URI Component encoded query string.
 */
export default function encode(object: any, keepArrayIndex?: boolean): string;
